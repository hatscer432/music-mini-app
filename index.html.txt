<!DOCTYPE html>
<html>
<head>
    <title>Music App</title>
    <style>
        body {
            background: linear-gradient(135deg, #56b7ee, #76d2a2);
            font-family: Arial;
            padding: 20px;
        }
        .box {
            background: white;
            padding: 20px;
            border-radius: 15px;
            max-width: 400px;
            margin: auto;
        }
        input, button {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border-radius: 8px;
            border: 1px solid #ccc;
        }
        button {
            background: #56b7ee;
            color: white;
            border: none;
        }
    </style>
</head>
<body>
    <div class="box">
        <h2>🎵 Добавить музыку</h2>
        <input id="title" placeholder="Название">
        <input id="artist" placeholder="Исполнитель">
        <button onclick="send()">Добавить</button>
    </div>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script>
        let tg = Telegram.WebApp;
        tg.ready();
        
        function send() {
            let title = document.getElementById('title').value;
            let artist = document.getElementById('artist').value;
            
            if (title && artist) {
                tg.sendData(JSON.stringify({
                    action: 'add_track',
                    trackTitle: title,
                    trackArtist: artist
                }));
                tg.close();
            }
        }
    </script>
</body>
</html>